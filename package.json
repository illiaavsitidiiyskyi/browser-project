{
  "name": "browser-project",
  "version": "0.1.0",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "electron": "^31.0.0"
  }
}